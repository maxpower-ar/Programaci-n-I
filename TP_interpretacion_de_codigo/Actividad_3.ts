let temperatura = 25;
let esSoleado = true;
let hayViento = false;

if ((temperatura > 20 || esSoleado) && !hayViento) {
    console.log("Buen clima para salir");
} else {
    console.log("Mejor quedarse adentro");
}

// 1- Buen clima para salir
// 2- Mejor quedarse adentro
// 3- Mejor quedarse adentro
// 4- Si, hay posibilidades, y puede ser porque hay temperatura > 20 y no hay viento o si esta soleado y no hay viento
